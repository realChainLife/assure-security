/// <reference types="node" />
import { pipeline, Readable, Writable, WritableOptions } from "stream";
export declare const pipelineAsync: typeof pipeline.__promisify__;
export declare class MemoryStream extends Writable {
    buffers: Buffer[];
    constructor(opts?: WritableOptions);
    _write(chunk: any, encoding: string, callback: (error?: Error | null) => void): void;
    getData(): Buffer;
}
export declare function readStream(stream: Readable, ignoreErrors?: boolean): Promise<Buffer>;
