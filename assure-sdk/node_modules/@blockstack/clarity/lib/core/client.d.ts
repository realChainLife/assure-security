import { Method, Query, Receipt, Transaction } from ".";
import { Provider } from "./provider";
export declare class Client {
    name: string;
    filePath: string;
    provider: Provider;
    constructor(name: string, filePath: string, provider: Provider);
    checkContract: () => Promise<void>;
    deployContract: () => Promise<Receipt>;
    createTransaction: (params?: {
        method?: Method | undefined;
    } | undefined) => Transaction;
    submitTransaction: (tx: Transaction) => Promise<Receipt>;
    createQuery: (params: {
        method?: Method | undefined;
        atChaintip?: boolean | undefined;
    }) => Query;
    submitQuery: (query: Query) => Promise<Receipt>;
}
